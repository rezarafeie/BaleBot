# BotMan PHP Hosting Rules
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirect index.html to index.php (optional but recommended for PHP hosts)
    RewriteRule ^index\.html$ /index.php [R=301,L]

    # Prevent directory browsing
    Options -Indexes

    # Deny access to sensitive files
    <FilesMatch "^\.env|config\.php|install\.sql|package\.json|vercel\.json">
        Order allow,deny
        Deny from all
    </FilesMatch>

    # Handle webhook with clean URLs (optional)
    # RewriteRule ^api/webhook$ webhook.php [L]
</IfModule>
